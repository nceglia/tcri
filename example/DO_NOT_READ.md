# 🚫 DO NOT READ / DO NOT USE THIS DIRECTORY 🚫

Every notebook in `example/` is DISPOSABLE and calls an OLD tcri API.

**Rules (see docs/contract/REFACTOR_HISTORY.md):** never read these for any decision; never move package functions here; `examples/` is not a destination; notebook usage is NOT evidence to keep a function.
